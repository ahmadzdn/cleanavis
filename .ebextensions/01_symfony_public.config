# Racine web = dossier public/ (front controller Symfony).
# Si ta pile EB ne prend pas en charge cette clé (selon la version de plateforme),
# configure la racine dans la console EB ou via .platform/nginx/ sur AL2023.
option_settings:
  aws:elasticbeanstalk:container:php:
    document_root: public
