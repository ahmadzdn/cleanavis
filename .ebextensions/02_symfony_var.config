# Après déploiement du bundle : dossiers Symfony accessibles en écriture.
container_commands:
  01_symfony_var:
    command: |
      mkdir -p var/cache var/log var/share
      chmod -R ug+rwx var || chmod -R 775 var
