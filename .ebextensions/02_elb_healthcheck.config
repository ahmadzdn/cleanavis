# Fichier statique : fonctionne sans routing Symfony / try_files (voir public/eb-health.txt).
# Évite les 404 ELB quand /health était résolu comme fichier disque.
option_settings:
  aws:elasticbeanstalk:environment:process:default:
    HealthCheckPath: /eb-health.txt
