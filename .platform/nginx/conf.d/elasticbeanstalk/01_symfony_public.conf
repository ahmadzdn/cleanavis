# Symfony : front controller dans public/ (Amazon Linux 2023 — pas d’option document_root PHP).
# Ce dossier est inclus dans le bloc server par EB.
root /var/app/current/public;
index index.php;

location / {
    try_files $uri /index.php$is_args$args;
}
