# Sans ceci, nginx cherche un fichier physique « public/health » → 404 au lieu de Symfony.
# document_root est déjà défini par la plateforme (public/).
location / {
    try_files $uri /index.php$is_args$args;
}
