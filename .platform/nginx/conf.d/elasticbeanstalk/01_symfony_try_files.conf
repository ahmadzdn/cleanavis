# Routage front-controller Symfony. Ne pas ajouter de directive « root » ici :
# EB définit déjà root/document_root dans elasticbeanstalk/php.conf — un second « root » provoque [emerg] duplicate.
location / {
    try_files $uri /index.php$is_args$args;
}
